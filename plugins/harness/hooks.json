{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Shell|exec_command",
        "hooks": [
          {
            "type": "command",
            "command": "printf '%s' \"${CODEX_TOOL_INPUT:-${TOOL_INPUT:-}}\" | grep -qE 'rm\\s+-rf|git\\s+push\\s+--force|git\\s+reset\\s+--hard|DROP\\s+TABLE' && { echo 'BLOCKED: 위험한 명령어가 감지되었습니다.' >&2; exit 1; } || exit 0"
          }
        ]
      }
    ]
  }
}
